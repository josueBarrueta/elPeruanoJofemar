import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';
import { AuthService } from '../auth.service';

@Component({ selector: 'app-login', standalone: true, imports: [FormsModule], template: `<main class="login-shell"><form class="login-card" (ngSubmit)="login()"><span>Panel privado</span><h1>Acceso de administración</h1><label>Correo<input type="email" name="email" [(ngModel)]="email" required></label><label>Contraseña<div class="password-wrap"><input [type]="showPassword ? 'text' : 'password'" name="password" [(ngModel)]="password" required><button type="button" class="password-toggle" (click)="showPassword = !showPassword" [attr.aria-label]="showPassword ? 'Ocultar contraseña' : 'Mostrar contraseña'">{{ showPassword ? '◉' : '◌' }}</button></div></label><p [hidden]="!error">{{error}}</p><button class="submit-button" type="submit">Entrar</button><a href="/">← Volver a la web</a></form></main>`, styles: [`.login-shell{min-height:100vh;display:grid;place-items:center;background:#f7f4f0;padding:1rem}.login-card{width:min(420px,100%);box-sizing:border-box;background:#fff;padding:2rem;border-radius:18px;box-shadow:0 12px 32px #48271812}.login-card label{display:grid;gap:.4rem;margin:1rem 0;font-weight:700}.login-card input{box-sizing:border-box;width:100%;padding:.75rem;border:1px solid #ded5ce;border-radius:10px;font:inherit}.password-wrap{position:relative}.password-wrap input{padding-right:3rem}.password-toggle{position:absolute;right:.35rem;top:50%;transform:translateY(-50%);padding:.35rem .5rem;border:0;background:transparent;color:#a92136;cursor:pointer;font-size:1.1rem}.submit-button{width:100%;padding:.85rem;border:0;border-radius:9px;background:#b1283f;color:#fff;font-weight:700}.login-card p{color:#b1283f}.login-card a{display:block;margin-top:1rem;text-align:center;color:#a92136}`] })
export class LoginComponent { private auth=inject(AuthService); private router=inject(Router); email=''; password=''; error=''; showPassword=false; login():void { this.auth.login(this.email,this.password).subscribe({next:()=>this.router.navigate(['/admin']),error:()=>this.error='Credenciales incorrectas'}); } }
